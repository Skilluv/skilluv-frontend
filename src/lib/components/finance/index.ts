/**
 * The two lines where somebody trades something durable for something
 * immediate: a slice of an invoice for cash now, a slice of their record for a
 * share of what it earns. Both state the cost before the gesture.
 */
export { default as AdvancesPanel } from './AdvancesPanel.svelte';
export { default as DataConsentPanel } from './DataConsentPanel.svelte';
